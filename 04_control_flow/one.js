// if

// const temperature = 45

// if(temperature === 50)
//         console.log("temperature is 50");
// else    
//     console.log('temperature is less than 50')

// >, <, ==, >=, <=, !=, ===, !==

// console.log(3 !== 3);

if(2 != 3){
    const web = 'youtube'
    // console.log(`welcome to ${web}`);
}

// console.log(web);

if( 2 == 2) console.log('true'), console.log('true');  // we can write multiple statement using , under a conditional 
    // statements but it is not good practice.

// Similarly, like java we can write multiple if , else , elseif etc.

// also like java , there are && and || operators in js , used in same way.
